# ARIA Backend Requirements
# Core Web Framework
Flask==3.0.0
Flask-CORS==4.0.0

# HTTP Client
requests==2.31.0

# Data Processing
python-dotenv==1.0.0

# Cloud Database (PostgreSQL Driver)
# Se usa psycopg (v3) en lugar de psycopg2 por ser una dependencia pura de Python,
# evitando problemas de compilación en Windows y con versiones nuevas de Python.
psycopg==3.1.18

# Text-to-Speech (Síntesis de voz Spock)
pyttsx3==2.90

# Production Server
gunicorn==21.2.0

# Development Tools
pytest==7.4.0
pytest-flask==1.3.0

# Dictionary Learning System Dependencies
# threading, pathlib y sqlite3 son módulos integrados de Python.

# Optional: For advanced AI features (future)
# numpy==1.24.3
# scikit-learn==1.3.0
# tensorflow==2.13.0
